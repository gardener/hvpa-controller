
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: hvpas.autoscaling.gardener.cloud
spec:
  group: autoscaling.gardener.cloud
  names:
    kind: Hvpa
    listKind: HvpaList
    plural: hvpas
    singular: hvpa
  scope: Namespaced
  subresources:
    scale:
      labelSelectorPath: .status.targetSelector
      specReplicasPath: .spec.replicas
      statusReplicasPath: .status.replicas
    status: {}
  validation:
    openAPIV3Schema:
      description: Hvpa is the Schema for the hvpas API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: HvpaSpec defines the desired state of Hvpa
          properties:
            baseResourcesPerReplica:
              additionalProperties:
                description: ChangeParams defines the parameters for scaling
                properties:
                  percentage:
                    description: Percentage is the percentage of currently allocated
                      value to be used for scaling
                    format: int32
                    type: integer
                  value:
                    description: Value is the absolute value of the scaling
                    type: string
                type: object
              description: BaseResourcesPerReplica specifies base resources to be
                budgeted per replica while scaling horizontally
              type: object
            hpa:
              description: Hpa defines the spec of HPA
              properties:
                deploy:
                  description: Deploy defines whether the HPA is deployed or not
                  type: boolean
                selector:
                  description: 'Selector is a label query that should match HPA. Must
                    match in order to be controlled. If empty, defaulted to labels
                    on HPA template. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors'
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: A label selector requirement is a selector that
                          contains values, a key, and an operator that relates the
                          key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: operator represents a key's relationship
                              to a set of values. Valid operators are In, NotIn, Exists
                              and DoesNotExist.
                            type: string
                          values:
                            description: values is an array of string values. If the
                              operator is In or NotIn, the values array must be non-empty.
                              If the operator is Exists or DoesNotExist, the values
                              array must be empty. This array is replaced during a
                              strategic merge patch.
                            items:
                              type: string
                            type: array
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: matchLabels is a map of {key,value} pairs. A single
                        {key,value} in the matchLabels map is equivalent to an element
                        of matchExpressions, whose key field is "key", the operator
                        is "In", and the values array contains only "value". The requirements
                        are ANDed.
                      type: object
                  type: object
                template:
                  description: Template is the object that describes the HPA that
                    will be created.
                  properties:
                    metadata:
                      description: Metadata of the pods created from this template.
                      type: object
                    spec:
                      description: Spec defines the behavior of a HPA.
                      properties:
                        maxReplicas:
                          description: maxReplicas is the upper limit for the number
                            of replicas to which the autoscaler can scale up. It cannot
                            be less that minReplicas.
                          format: int32
                          type: integer
                        metrics:
                          description: metrics contains the specifications for which
                            to use to calculate the desired replica count (the maximum
                            replica count across all metrics will be used).  The desired
                            replica count is calculated multiplying the ratio between
                            the target value and the current value by the current
                            number of pods.  Ergo, metrics used must decrease as the
                            pod count is increased, and vice-versa.  See the individual
                            metric source types for more information about how each
                            type of metric must respond. If not set, the default metric
                            will be set to 80% average CPU utilization.
                          items:
                            description: MetricSpec specifies how to scale based on
                              a single metric (only `type` and one other matching
                              field should be set at once).
                            properties:
                              external:
                                description: external refers to a global metric that
                                  is not associated with any Kubernetes object. It
                                  allows autoscaling based on information coming from
                                  components running outside of cluster (for example
                                  length of queue in cloud messaging service, or QPS
                                  from loadbalancer running outside of cluster).
                                properties:
                                  metricName:
                                    description: metricName is the name of the metric
                                      in question.
                                    type: string
                                  metricSelector:
                                    description: metricSelector is used to identify
                                      a specific time series within a given metric.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: A label selector requirement
                                            is a selector that contains values, a
                                            key, and an operator that relates the
                                            key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: operator represents a key's
                                                relationship to a set of values. Valid
                                                operators are In, NotIn, Exists and
                                                DoesNotExist.
                                              type: string
                                            values:
                                              description: values is an array of string
                                                values. If the operator is In or NotIn,
                                                the values array must be non-empty.
                                                If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This
                                                array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: matchLabels is a map of {key,value}
                                          pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions,
                                          whose key field is "key", the operator is
                                          "In", and the values array contains only
                                          "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                  targetAverageValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: targetAverageValue is the target
                                      per-pod value of global metric (as a quantity).
                                      Mutually exclusive with TargetValue.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  targetValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: targetValue is the target value of
                                      the metric (as a quantity). Mutually exclusive
                                      with TargetAverageValue.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - metricName
                                type: object
                              object:
                                description: object refers to a metric describing
                                  a single kubernetes object (for example, hits-per-second
                                  on an Ingress object).
                                properties:
                                  averageValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: averageValue is the target value
                                      of the average of the metric across all relevant
                                      pods (as a quantity)
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  metricName:
                                    description: metricName is the name of the metric
                                      in question.
                                    type: string
                                  selector:
                                    description: selector is the string-encoded form
                                      of a standard kubernetes label selector for
                                      the given metric When set, it is passed as an
                                      additional parameter to the metrics server for
                                      more specific metrics scoping When unset, just
                                      the metricName will be used to gather metrics.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: A label selector requirement
                                            is a selector that contains values, a
                                            key, and an operator that relates the
                                            key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: operator represents a key's
                                                relationship to a set of values. Valid
                                                operators are In, NotIn, Exists and
                                                DoesNotExist.
                                              type: string
                                            values:
                                              description: values is an array of string
                                                values. If the operator is In or NotIn,
                                                the values array must be non-empty.
                                                If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This
                                                array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: matchLabels is a map of {key,value}
                                          pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions,
                                          whose key field is "key", the operator is
                                          "In", and the values array contains only
                                          "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                  target:
                                    description: target is the described Kubernetes
                                      object.
                                    properties:
                                      apiVersion:
                                        description: API version of the referent
                                        type: string
                                      kind:
                                        description: 'Kind of the referent; More info:
                                          https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds"'
                                        type: string
                                      name:
                                        description: 'Name of the referent; More info:
                                          http://kubernetes.io/docs/user-guide/identifiers#names'
                                        type: string
                                    required:
                                    - kind
                                    - name
                                    type: object
                                  targetValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: targetValue is the target value of
                                      the metric (as a quantity).
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - metricName
                                - target
                                - targetValue
                                type: object
                              pods:
                                description: pods refers to a metric describing each
                                  pod in the current scale target (for example, transactions-processed-per-second).  The
                                  values will be averaged together before being compared
                                  to the target value.
                                properties:
                                  metricName:
                                    description: metricName is the name of the metric
                                      in question
                                    type: string
                                  selector:
                                    description: selector is the string-encoded form
                                      of a standard kubernetes label selector for
                                      the given metric When set, it is passed as an
                                      additional parameter to the metrics server for
                                      more specific metrics scoping When unset, just
                                      the metricName will be used to gather metrics.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: A label selector requirement
                                            is a selector that contains values, a
                                            key, and an operator that relates the
                                            key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: operator represents a key's
                                                relationship to a set of values. Valid
                                                operators are In, NotIn, Exists and
                                                DoesNotExist.
                                              type: string
                                            values:
                                              description: values is an array of string
                                                values. If the operator is In or NotIn,
                                                the values array must be non-empty.
                                                If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This
                                                array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: matchLabels is a map of {key,value}
                                          pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions,
                                          whose key field is "key", the operator is
                                          "In", and the values array contains only
                                          "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                  targetAverageValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: targetAverageValue is the target
                                      value of the average of the metric across all
                                      relevant pods (as a quantity)
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - metricName
                                - targetAverageValue
                                type: object
                              resource:
                                description: resource refers to a resource metric
                                  (such as those specified in requests and limits)
                                  known to Kubernetes describing each pod in the current
                                  scale target (e.g. CPU or memory). Such metrics
                                  are built in to Kubernetes, and have special scaling
                                  options on top of those available to normal per-pod
                                  metrics using the "pods" source.
                                properties:
                                  name:
                                    description: name is the name of the resource
                                      in question.
                                    type: string
                                  targetAverageUtilization:
                                    description: targetAverageUtilization is the target
                                      value of the average of the resource metric
                                      across all relevant pods, represented as a percentage
                                      of the requested value of the resource for the
                                      pods.
                                    format: int32
                                    type: integer
                                  targetAverageValue:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: targetAverageValue is the target
                                      value of the average of the resource metric
                                      across all relevant pods, as a raw value (instead
                                      of as a percentage of the request), similar
                                      to the "pods" metric source type.
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - name
                                type: object
                              type:
                                description: type is the type of metric source.  It
                                  should be one of "Object", "Pods" or "Resource",
                                  each mapping to a matching field in the object.
                                type: string
                            required:
                            - type
                            type: object
                          type: array
                        minReplicas:
                          description: minReplicas is the lower limit for the number
                            of replicas to which the autoscaler can scale down. It
                            defaults to 1 pod.
                          format: int32
                          type: integer
                      required:
                      - maxReplicas
                      type: object
                  type: object
              type: object
            maintenanceTimeWindow:
              description: MaintenanceTimeWindow contains information about the time
                window for maintenance operations.
              properties:
                begin:
                  description: Begin is the beginning of the time window in the format
                    HHMMSS+ZONE, e.g. "220000+0100".
                  type: string
                end:
                  description: End is the end of the time window in the format HHMMSS+ZONE,
                    e.g. "220000+0100".
                  type: string
              required:
              - begin
              - end
              type: object
            replicas:
              description: Replicas is the number of replicas of target resource
              format: int32
              type: integer
            scaleDown:
              description: ScaleDown defines the parameters for scale down
              properties:
                minChange:
                  description: MinChange is the minimum change in the resource on
                    which HVPA acts HVPA uses minimum of the Value and Percentage
                    value
                  properties:
                    cpu:
                      description: Scale parameters for CPU
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    memory:
                      description: Scale parameters for memory
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    replicas:
                      description: Scale patameters for replicas
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                  type: object
                stabilizationDuration:
                  description: StabilizationDuration defines the minimum delay in
                    minutes between 2 consecutive scale operations Valid time units
                    are "ns", "us" (or "µs"), "ms", "s", "m", "h"
                  type: string
                updatePolicy:
                  description: Describes the rules on when changes are applied. If
                    not specified, all fields in the `UpdatePolicy` are set to their
                    default values.
                  properties:
                    updateMode:
                      description: Controls when autoscaler applies changes to the
                        resources. The default is 'Auto'.
                      type: string
                  type: object
              type: object
            scaleIntervals:
              description: ScalingIntervals indirectly define potentially multiple
                intervals in total resources for each of which there is a corresponding
                well-defined single desired "replicas" value. The intervals are expected
                not to have gaps and any overlap is determinied by explicit ScalingIntervalsOverlap
                configuration or any other implicit form of hysteresis.
              items:
                description: ScaleIntervals defines the scaling interval for resources
                properties:
                  effectiveIntervalExtrapolation:
                    description: This field defines how to compute multiple effective
                      scaling intervals from the total resource interval defined by
                      this interval. Default is 'linear'
                    type: string
                  maxCpu:
                    description: Scale parameters for CPU
                    type: string
                  maxMemory:
                    description: Scale parameters for memory
                    type: string
                  maxReplicas:
                    description: Scale patameters for replicas
                    format: int32
                    type: integer
                type: object
              type: array
            scaleUp:
              description: ScaleUp defines the parameters for scale up
              properties:
                minChange:
                  description: MinChange is the minimum change in the resource on
                    which HVPA acts HVPA uses minimum of the Value and Percentage
                    value
                  properties:
                    cpu:
                      description: Scale parameters for CPU
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    memory:
                      description: Scale parameters for memory
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    replicas:
                      description: Scale patameters for replicas
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                  type: object
                stabilizationDuration:
                  description: StabilizationDuration defines the minimum delay in
                    minutes between 2 consecutive scale operations Valid time units
                    are "ns", "us" (or "µs"), "ms", "s", "m", "h"
                  type: string
                updatePolicy:
                  description: Describes the rules on when changes are applied. If
                    not specified, all fields in the `UpdatePolicy` are set to their
                    default values.
                  properties:
                    updateMode:
                      description: Controls when autoscaler applies changes to the
                        resources. The default is 'Auto'.
                      type: string
                  type: object
              type: object
            scalingIntervalsOverlap:
              additionalProperties:
                description: ChangeParams defines the parameters for scaling
                properties:
                  percentage:
                    description: Percentage is the percentage of currently allocated
                      value to be used for scaling
                    format: int32
                    type: integer
                  value:
                    description: Value is the absolute value of the scaling
                    type: string
                type: object
              description: ScalingIntervalsOverlap is used to compute the effective
                scaling intervals
              type: object
            targetRef:
              description: TargetRef points to the controller managing the set of
                pods for the autoscaler to control
              properties:
                apiVersion:
                  description: API version of the referent
                  type: string
                kind:
                  description: 'Kind of the referent; More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds"'
                  type: string
                name:
                  description: 'Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names'
                  type: string
              required:
              - kind
              - name
              type: object
            vpa:
              description: Vpa defines the spec of VPA
              properties:
                deploy:
                  description: Deploy defines whether the VPA is deployed or not
                  type: boolean
                limitsRequestsGapScaleParams:
                  description: LimitsRequestsGapScaleParams is the scaling thresholds
                    for limits
                  properties:
                    cpu:
                      description: Scale parameters for CPU
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    memory:
                      description: Scale parameters for memory
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                    replicas:
                      description: Scale patameters for replicas
                      properties:
                        percentage:
                          description: Percentage is the percentage of currently allocated
                            value to be used for scaling
                          format: int32
                          type: integer
                        value:
                          description: Value is the absolute value of the scaling
                          type: string
                      type: object
                  type: object
                selector:
                  description: 'Selector is a label query that should match VPA. Must
                    match in order to be controlled. If empty, defaulted to labels
                    on VPA template. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors'
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: A label selector requirement is a selector that
                          contains values, a key, and an operator that relates the
                          key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: operator represents a key's relationship
                              to a set of values. Valid operators are In, NotIn, Exists
                              and DoesNotExist.
                            type: string
                          values:
                            description: values is an array of string values. If the
                              operator is In or NotIn, the values array must be non-empty.
                              If the operator is Exists or DoesNotExist, the values
                              array must be empty. This array is replaced during a
                              strategic merge patch.
                            items:
                              type: string
                            type: array
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: matchLabels is a map of {key,value} pairs. A single
                        {key,value} in the matchLabels map is equivalent to an element
                        of matchExpressions, whose key field is "key", the operator
                        is "In", and the values array contains only "value". The requirements
                        are ANDed.
                      type: object
                  type: object
                template:
                  description: Template is the object that describes the VPA that
                    will be created.
                  properties:
                    metadata:
                      description: Metadata of the pods created from this template.
                      type: object
                    spec:
                      description: Spec defines the behavior of a VPA.
                      properties:
                        resourcePolicy:
                          description: Controls how the autoscaler computes recommended
                            resources. The resource policy may be used to set constraints
                            on the recommendations for individual containers. If not
                            specified, the autoscaler computes recommended resources
                            for all containers in the pod, without additional constraints.
                          properties:
                            containerPolicies:
                              description: Per-container resource policies.
                              items:
                                description: ContainerResourcePolicy controls how
                                  autoscaler computes the recommended resources for
                                  a specific container.
                                properties:
                                  containerName:
                                    description: Name of the container or DefaultContainerResourcePolicy,
                                      in which case the policy is used by the containers
                                      that don't have their own policy specified.
                                    type: string
                                  maxAllowed:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: Specifies the maximum amount of resources
                                      that will be recommended for the container.
                                      The default is no maximum.
                                    type: object
                                  minAllowed:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: Specifies the minimal amount of resources
                                      that will be recommended for the container.
                                      The default is no minimum.
                                    type: object
                                  mode:
                                    description: Whether autoscaler is enabled for
                                      the container. The default is "Auto".
                                    type: string
                                type: object
                              type: array
                          type: object
                      type: object
                  type: object
              type: object
          required:
          - targetRef
          type: object
        status:
          description: HvpaStatus defines the observed state of Hvpa
          properties:
            lastBlockedScaling:
              items:
                description: BlockedScaling defines the details for blocked scaling
                properties:
                  reason:
                    description: BlockingReason defines the reason for blocking.
                    type: string
                  scalingStatus:
                    description: ScalingStatus defines the status of scaling
                    properties:
                      hpaStatus:
                        description: HpaStatus defines the status of HPA
                        properties:
                          currentReplicas:
                            format: int32
                            type: integer
                          desiredReplicas:
                            format: int32
                            type: integer
                        type: object
                      lastUpdated:
                        format: date-time
                        type: string
                      vpaStatus:
                        description: VpaStatus defines the status of VPA
                        properties:
                          containerResources:
                            items:
                              description: ContainerResources has the details of the
                                container resources
                              properties:
                                containerName:
                                  description: Name of the container.
                                  type: string
                                resources:
                                  description: ResourceRequirements describes the
                                    compute resource requirements.
                                  properties:
                                    limits:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Limits describes the maximum amount
                                        of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                    requests:
                                      additionalProperties:
                                        anyOf:
                                        - type: integer
                                        - type: string
                                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                        x-kubernetes-int-or-string: true
                                      description: 'Requests describes the minimum
                                        amount of compute resources required. If Requests
                                        is omitted for a container, it defaults to
                                        Limits if that is explicitly specified, otherwise
                                        to an implementation-defined value. More info:
                                        https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                      type: object
                                  type: object
                              type: object
                            type: array
                        type: object
                    type: object
                type: object
              type: array
            lastError:
              description: LastError has details of any errors that occurred
              properties:
                description:
                  description: Description of the error
                  type: string
                lastOperation:
                  description: LastOperation is the type of operation for which error
                    occurred
                  type: string
                lastUpdateTime:
                  description: Time at which the error occurred
                  format: date-time
                  type: string
              type: object
            lastProcessedRecommendations:
              description: ScalingStatus defines the status of scaling
              properties:
                hpaStatus:
                  description: HpaStatus defines the status of HPA
                  properties:
                    currentReplicas:
                      format: int32
                      type: integer
                    desiredReplicas:
                      format: int32
                      type: integer
                  type: object
                lastUpdated:
                  format: date-time
                  type: string
                vpaStatus:
                  description: VpaStatus defines the status of VPA
                  properties:
                    containerResources:
                      items:
                        description: ContainerResources has the details of the container
                          resources
                        properties:
                          containerName:
                            description: Name of the container.
                            type: string
                          resources:
                            description: ResourceRequirements describes the compute
                              resource requirements.
                            properties:
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Limits describes the maximum amount
                                  of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Requests describes the minimum amount
                                  of compute resources required. If Requests is omitted
                                  for a container, it defaults to Limits if that is
                                  explicitly specified, otherwise to an implementation-defined
                                  value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                type: object
                            type: object
                        type: object
                      type: array
                  type: object
              type: object
            lastScaling:
              description: ScalingStatus defines the status of scaling
              properties:
                hpaStatus:
                  description: HpaStatus defines the status of HPA
                  properties:
                    currentReplicas:
                      format: int32
                      type: integer
                    desiredReplicas:
                      format: int32
                      type: integer
                  type: object
                lastUpdated:
                  format: date-time
                  type: string
                vpaStatus:
                  description: VpaStatus defines the status of VPA
                  properties:
                    containerResources:
                      items:
                        description: ContainerResources has the details of the container
                          resources
                        properties:
                          containerName:
                            description: Name of the container.
                            type: string
                          resources:
                            description: ResourceRequirements describes the compute
                              resource requirements.
                            properties:
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Limits describes the maximum amount
                                  of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: 'Requests describes the minimum amount
                                  of compute resources required. If Requests is omitted
                                  for a container, it defaults to Limits if that is
                                  explicitly specified, otherwise to an implementation-defined
                                  value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                                type: object
                            type: object
                        type: object
                      type: array
                  type: object
              type: object
            overrideScaleUpStabilization:
              description: Override scale up stabilization
              type: boolean
            replicas:
              description: Replicas is the number of replicas of the target resource.
              format: int32
              type: integer
            scaleDownUpdatePolicy:
              description: Current UpdatePolicy set in the spec
              properties:
                updateMode:
                  description: Controls when autoscaler applies changes to the resources.
                    The default is 'Auto'.
                  type: string
              type: object
            scaleUpUpdatePolicy:
              description: Current UpdatePolicy set in the spec
              properties:
                updateMode:
                  description: Controls when autoscaler applies changes to the resources.
                    The default is 'Auto'.
                  type: string
              type: object
            targetSelector:
              description: TargetSelector is the string form of the label selector
                of HPA. This is required for HPA to work with scale subresource.
              type: string
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
